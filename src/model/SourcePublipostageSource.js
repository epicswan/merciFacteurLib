/*
 * Merci facteur API
 * Merci facteur PRO vous permet d'envoyer des courriers depuis votre applicatif via son API.     Types de courriers disponibles :   - Lettre de une ou plusieurs pages.  - Carte postale avec ou sans enveloppe  - Carte pliée  - Carte géante  - Carte non pliée  - Photos sur papier brillant    Modes d'envois disponibles :   - Envoi normal (lettre verte)  - Lettre suivie  - Recommandé avec avis de réception      Au sujet des webhooks : [`https://github.com/MerciFacteur/Merci-facteur-API/blob/master/README.md#webhooks`](https://github.com/MerciFacteur/Merci-facteur-API/blob/master/README.md#webhooks)     Librairie PHP et exemples d'intégration : [`https://github.com/MerciFacteur/Merci-facteur-API`](https://github.com/MerciFacteur/Merci-facteur-API)    Infos générales sur l'API : [`https://github.com/MerciFacteur/Merci-facteur-API/blob/master/README.md`](https://github.com/MerciFacteur/Merci-facteur-API/blob/master/README.md)    Plus d'informations sur [`https://www.merci-facteur.com/pro`](https://www.merci-facteur.com/pro)    N'hésitez pas à nous contacter via [`https://www.merci-facteur.com/pro/contact.php`](https://www.merci-facteur.com/pro/contact.php)
 *
 * OpenAPI spec version: 1.2.7
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.50
 *
 * Do not edit the class manually.
 *
 */
import {ApiClient} from '../ApiClient';

/**
 * The SourcePublipostageSource model module.
 * @module model/SourcePublipostageSource
 * @version 1.2.7
 */
export class SourcePublipostageSource {
  /**
   * Constructs a new <code>SourcePublipostageSource</code>.
   * URL du fichier csv/txt, sa version encodée en base64, ou le json des données.
   * @alias module:model/SourcePublipostageSource
   * @class
   */
  constructor() {
  }

  /**
   * Constructs a <code>SourcePublipostageSource</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/SourcePublipostageSource} obj Optional instance to populate.
   * @return {module:model/SourcePublipostageSource} The populated <code>SourcePublipostageSource</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new SourcePublipostageSource();
      if (data.hasOwnProperty('type'))
        obj.type = ApiClient.convertToType(data['type'], 'String');
      if (data.hasOwnProperty('value'))
        obj.value = ApiClient.convertToType(data['value'], 'String');
    }
    return obj;
  }
}

/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
SourcePublipostageSource.TypeEnum = {
  /**
   * value: "file"
   * @const
   */
  file: "file",

  /**
   * value: "base64"
   * @const
   */
  base64: "base64",

  /**
   * value: "json"
   * @const
   */
  json: "json"
};
/**
 * Type de source envoyée (\"file\" pour une URL vers un fichier CSV ou TXT, \"base64\" pour un fichier CSV ou TXT encodé en base64, ou \"json\" pour un json).
 * @member {module:model/SourcePublipostageSource.TypeEnum} type
 */
SourcePublipostageSource.prototype.type = undefined;

/**
 * Si type=\"file\", l'URL du fichier. Si type=\"base64\", la base64. Si type=\"json\", un json contenant les données sources ([{destinataire 1},{destinataire 2},etc...])
 * @member {String} value
 */
SourcePublipostageSource.prototype.value = undefined;

